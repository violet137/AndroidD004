<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Toolbar -->
    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_height"
        android:background="@color/colorPrimary">
        <TextView
            android:id="@+id/tvTiltleDangNhap_FragmentDangNhap"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="SHOPPING"
            android:textSize="20sp"
            android:textStyle="italic"
            android:typeface="serif" />
    </android.support.v7.widget.Toolbar>

    <!-- Thay fragment -->
    <TextView
        android:id="@+id/tvName_contnt_main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/cardview_light_background"
        android:background="@color/cardview_dark_background"
        android:textStyle="bold"
        android:textSize="@dimen/text_size_15dp"
        android:paddingStart="@dimen/textView_pandding_left_15dp"
        android:paddingTop="@dimen/textView_pandding_top_3dp"
        android:paddingBottom="@dimen/textView_pandding_bottom_3dp"
        android:text="TextView"
        android:visibility="gone"/>

    <FrameLayout
        android:id="@+id/content_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</LinearLayout>