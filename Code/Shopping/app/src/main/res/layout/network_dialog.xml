<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/shape_khung"
    android:layout_gravity="center"
    android:paddingBottom="20dp">

    <!-- Message -->
    <TextView
        android:id="@+id/message_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_margin="@dimen/message_dialog_margin"
        android:text="Test message"
        android:textColor="@color/colorTextDark"
        android:textSize="@dimen/message_dialog_text_size"/>

    <!-- Nút xác nhận -->
    <Button
        android:id="@+id/btn_xac_nhan_message_dialog"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/button_xac_nhan_height"
        android:background="@drawable/shape_khung"
        android:layout_gravity="center"
        android:text="Thử lại"
        android:textSize="@dimen/button_text_size"/>
</LinearLayout>